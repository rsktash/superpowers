# superpowers-bqp — skill edits for typed forks, lane handoffs, tracker mining, and delegated fix routing

Spec bead: `superpowers-bqp` (Workflow Fix Plan item 5; ruling R-32 on superpowers-5f7). Brainstormed 2026-09-04.

**Summary.** Five plugin text changes and one docs task, each rewritten as a whole section inside its word budget, plus release 1.4.49. writing-plans files a fork as a "Decide:" bead with its question and topic in one transaction, run under `BD_ACTOR=planner` so the byline says who wrote it. The handoff skill moves from anchor-bead comments to bd execution plans: one plan per project, one lane per epic created and claimed by the coordinator at execution start through a shared step the three execution skills cite, a typed `bd plan handoff` at Close, `bd plan show` at Resume, and a SessionStart hook that exports `BD_SESSION_ID`. A new forked, read-only tracker-mining skill returns one fixed brief shape with proposed bd commands. Fix Routing sends every behavioural fix to the implementer's live session and leaves the controller mechanical edits only. Every charter teaches the topic pick from the printed catalogue; executors never mint.

**Key decisions (owner, 2026-09-04).** Planner actor word is `planner`. Lane creation is the coordinator's job at execution start. A lane-less session posts no record at Close. Topic minting is restricted to owner, coordinator, and planner. Controller inline scope is mechanical edits only. The six structural-index script proposals, search indexing, and semantic retrieval wait until this epic has run on the new machinery and been measured.

**Acceptance.** Nine criteria on the bead: the rewritten sections quote the verified bd commands and stay inside their budgets; seven handoff scenarios, one planner scenario, three mining scenarios, two fix-routing scenarios, and one implementer scenario pass as fresh-agent runs; the SessionStart hook has a deterministic test; the deterministic suite exits 0; version 1.4.49 in all five files with a changelog entry stating the word delta.
